[project]
name = "receipt-scanner"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "flask>=3.1.3",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
]

[tool.pytest.ini_options]
pythonpath = ["."]
